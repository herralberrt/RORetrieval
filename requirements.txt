# Core Dependencies
python-dotenv==1.0.0
pyyaml==6.0.1

# NLP & Embeddings
# transformers >= 4.50 is required for Gemma 3 (query generation).
transformers>=4.51.0
sentence-transformers>=3.0.0
datasets>=2.18.0
tokenizers>=0.21.0

# Query generation with Gemma 3 runs inside the Apptainer image
# (see containers/README.md), which installs torch + vLLM at pinned versions.
# For a bare venv on a GPU machine, add:
#   torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#   vllm==0.8.5.post1
accelerate>=1.0.0
sentencepiece>=0.2.0
huggingface-hub>=0.30.0

# Ranking & Similarity
rank-bm25==0.2.2
scikit-learn==1.4.0
numpy==1.24.0
scipy==1.12.0

# LLM APIs
openai==1.3.0
anthropic==0.7.0

# Data Processing
pandas==2.1.0
jsonlines==4.0.0
tqdm==4.67.0

# Evaluation Metrics
rouge-score==0.1.2
nltk==3.8.1

# Utilities
requests==2.31.0
matplotlib==3.8.0
seaborn==0.13.0
feedparser==6.0.10
beautifulsoup4==4.12.2
